# 测试用例设计文档

## 一、测试范围

本项目针对 GitHub API 和 GitHub Web 进行自动化测试，覆盖以下功能模块：

### API 测试范围

| 模块 | 测试点 | 优先级 |
|------|--------|--------|
| 用户功能 | 获取用户信息、获取认证用户、获取仓库列表 | P0 |
| 仓库功能 | 获取仓库详情、处理不存在仓库 | P0 |
| 搜索功能 | 搜索仓库、空结果处理 | P1 |
| Issue 功能 | 获取 Issue 列表、创建 Issue、状态筛选 | P1 |
| 频率限制 | 获取 API 频率限制状态 | P2 |
| 高级特性 | 熔断器、限流器、插件系统、安全模块 | P1 |
| 性能测试 | 响应时间、并发请求 | P2 |
| 压力测试 | 高并发、持续压力、负载测试 | P2 |

### Web UI 测试范围

| 模块 | 测试点 | 优先级 |
|------|--------|--------|
| 登录功能 | 登录成功、登录失败、表单验证 | P0 |
| 首页功能 | 页面加载、导航、搜索、仓库列表 | P1 |
| 用户操作 | 个人中心、仓库创建、设置 | P1 |

## 二、测试用例清单

### 2.1 用户相关功能测试

| 用例ID | 用例名称 | 测试步骤 | 预期结果 | 优先级 |
|--------|----------|----------|----------|--------|
| TC-USER-001 | 获取指定用户信息 | 1. 调用 GET /users/{username}<br>2. 验证响应状态码 200<br>3. 验证响应字段完整性 | 返回正确的用户信息，包含 login、id、avatar_url 等字段 | P0 |
| TC-USER-002 | 获取当前认证用户 | 1. 调用 GET /user<br>2. 验证响应状态码 200<br>3. 验证返回当前用户信息 | 返回认证用户的完整信息 | P0 |
| TC-USER-003 | 获取用户公开仓库列表 | 1. 调用 GET /users/{username}/repos<br>2. 验证返回仓库列表<br>3. 验证仓库字段完整性 | 返回用户的公开仓库列表 | P1 |
| TC-USER-004 | 获取当前用户仓库列表 | 1. 调用 GET /user/repos<br>2. 验证返回包含私有仓库<br>3. 验证 visibility 参数过滤 | 返回当前用户的所有仓库 | P1 |

### 2.2 仓库相关功能测试

| 用例ID | 用例名称 | 测试步骤 | 预期结果 | 优先级 |
|--------|----------|----------|----------|--------|
| TC-REPO-001 | 获取指定仓库详情 | 1. 调用 GET /repos/{owner}/{repo}<br>2. 验证仓库基本信息<br>3. 验证仓库统计信息 | 返回仓库的完整信息 | P0 |
| TC-REPO-002 | 获取不存在的仓库 | 1. 调用不存在的仓库接口<br>2. 验证返回 404 状态码<br>3. 验证错误信息 | 返回 404 和正确的错误信息 | P1 |

### 2.3 搜索功能测试

| 用例ID | 用例名称 | 测试步骤 | 预期结果 | 优先级 |
|--------|----------|----------|----------|--------|
| TC-SEARCH-001 | 搜索仓库 | 1. 调用搜索接口<br>2. 验证返回搜索结果<br>3. 验证排序正确性 | 返回匹配的仓库列表，按指定字段排序 | P1 |
| TC-SEARCH-002 | 搜索返回空结果 | 1. 搜索不存在的关键词<br>2. 验证返回空列表<br>3. 验证 total_count 为 0 | 正确处理空搜索结果 | P2 |

### 2.4 Issue 相关功能测试

| 用例ID | 用例名称 | 测试步骤 | 预期结果 | 优先级 |
|--------|----------|----------|----------|--------|
| TC-ISSUE-001 | 获取仓库 Issue 列表 | 1. 调用获取 Issue 列表接口<br>2. 验证返回 Issue 列表<br>3. 验证 Issue 字段完整性 | 返回仓库的 Issue 列表 | P1 |
| TC-ISSUE-002 | 创建新 Issue | 1. 调用创建 Issue 接口<br>2. 验证返回 201 状态码<br>3. 验证 Issue 创建成功 | 成功创建 Issue 并返回 Issue 信息 | P0 |
| TC-ISSUE-003 | 按状态筛选 Issue | 1. 调用获取 Issue 接口，state=closed<br>2. 验证返回的都是关闭状态 | 返回指定状态的 Issue 列表 | P1 |

### 2.5 高级特性测试

| 用例ID | 用例名称 | 测试步骤 | 预期结果 | 优先级 |
|--------|----------|----------|----------|--------|
| TC-CB-001 | 熔断器初始状态 | 1. 创建熔断器实例<br>2. 验证初始状态为 CLOSED | 熔断器初始状态正确 | P0 |
| TC-CB-002 | 熔断器打开 | 1. 连续触发失败<br>2. 验证熔断器状态变为 OPEN | 达到阈值后熔断器打开 | P0 |
| TC-CB-003 | 熔断器快速失败 | 1. 熔断器打开后调用<br>2. 验证直接抛出异常 | 熔断状态下快速失败 | P0 |
| TC-CB-004 | 熔断器恢复 | 1. 等待超时时间<br>2. 调用成功请求<br>3. 验证状态恢复 CLOSED | 熔断器正确恢复 | P1 |
| TC-RL-001 | 限流器初始化 | 1. 创建限流器<br>2. 验证参数正确设置 | 限流器初始化成功 | P0 |
| TC-RL-002 | 限流器允许请求 | 1. 在限制内发送请求<br>2. 验证请求被允许 | 请求正常处理 | P0 |
| TC-RL-003 | 限流器拒绝超限请求 | 1. 超过限制发送请求<br>2. 验证请求被拒绝 | 超限请求被正确拒绝 | P0 |
| TC-SEC-001 | 敏感信息过滤 | 1. 日志包含敏感信息<br>2. 验证敏感信息被脱敏 | 日志中敏感信息被替换 | P0 |
| TC-SEC-002 | 数据加密解密 | 1. 加密敏感数据<br>2. 解密数据<br>3. 验证数据一致性 | 加密解密功能正常 | P0 |

### 2.6 Web UI 测试

| 用例ID | 用例名称 | 测试步骤 | 预期结果 | 优先级 |
|--------|----------|----------|----------|--------|
| TC-WEB-001 | 登录成功 | 1. 打开登录页面<br>2. 输入正确的用户名密码<br>3. 点击登录按钮 | 成功登录，跳转到首页 | P0 |
| TC-WEB-002 | 登录失败 | 1. 输入错误的用户名密码<br>2. 点击登录按钮 | 显示错误提示信息 | P0 |
| TC-WEB-003 | 登录页面元素验证 | 1. 打开登录页面<br>2. 验证各元素显示正确 | 用户名、密码输入框和登录按钮显示正确 | P1 |
| TC-WEB-004 | 首页加载 | 1. 登录后进入首页<br>2. 验证页面元素 | 首页正常加载，元素显示正确 | P0 |
| TC-WEB-005 | 导航功能 | 1. 点击导航链接<br>2. 验证页面跳转 | 正确跳转到对应页面 | P1 |
| TC-WEB-006 | 搜索功能 | 1. 输入搜索关键词<br>2. 提交搜索 | 显示搜索结果 | P1 |
| TC-WEB-007 | 仓库列表显示 | 1. 进入仓库列表页面<br>2. 验证仓库列表 | 仓库列表正确显示 | P1 |

## 三、测试数据设计

### 3.1 用户测试数据

```yaml
valid_users:
  - username: "octocat"
    expected_id: 583231
    expected_type: "User"
  - username: "torvalds"
    expected_type: "User"

invalid_users:
  - username: "nonexistent-user-xyz-12345"
    expected_status: 404
```

### 3.2 仓库测试数据

```yaml
valid_repos:
  - owner: "octocat"
    repo: "Hello-World"
    expected_language: null
  - owner: "django"
    repo: "django"
    expected_language: "Python"

invalid_repos:
  - owner: "nonexistent-owner"
    repo: "nonexistent-repo"
    expected_status: 404
```

### 3.3 Issue 测试数据

```yaml
new_issue:
  title: "Test Issue from Automation"
  body: "This is a test issue created by automation framework"
  labels: ["bug", "test"]

issue_filters:
  - state: "open"
    expected_count: ">0"
  - state: "closed"
    expected_count: ">=0"
```

### 3.4 Web 测试数据

```yaml
login_data:
  valid:
    username: "testuser"
    password: "testpassword"
  invalid:
    - username: "wronguser"
      password: "wrongpass"
      expected_error: "Incorrect username or password"

search_data:
  - keyword: "pytest"
    expected_min_results: 1
  - keyword: "nonexistent-repo-xyz-12345"
    expected_results: 0
```

## 四、测试策略

### 4.1 Mock 测试策略

由于 GitHub API 有频率限制，本项目采用 Mock 测试策略：

1. **单元测试**: 使用 Mock 模拟 API 响应，测试框架逻辑
2. **集成测试**: 使用 Mock 模拟完整流程，测试模块间交互
3. **边界测试**: 使用 Mock 模拟各种边界情况和异常场景

### 4.2 测试优先级

- **P0**: 核心功能，必须通过
- **P1**: 重要功能，应该通过
- **P2**: 次要功能，建议通过

### 4.3 测试覆盖率目标

| 指标 | 目标 |
|------|------|
| API 层覆盖率 | >= 95% |
| 公共组件覆盖率 | >= 80% |
| 用例通过率 | 100% |

## 五、测试执行

### 5.1 执行命令

```bash
# 运行所有测试
pytest

# 运行 API 测试
pytest test_cases/api/ -v

# 运行 Web UI 测试
pytest test_cases/web/ -v

# 生成 Allure 报告
pytest --alluredir=./allure-results
allure serve allure-results
```

### 5.2 CI/CD 集成

项目使用 GitHub Actions 进行持续集成，每次提交自动运行测试。
